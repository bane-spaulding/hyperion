- create a button to manually kick-off the experiment, it launches:
  - ViewMonitor
  - Sets title
  - Sets thumbnail

- make TokenRefresher a ParentModule to set credential before children get initialized

(optional)
- make centralized Agent to count quote limits
  -> take into account cost of uploading the image
  -> getting views every 30s

